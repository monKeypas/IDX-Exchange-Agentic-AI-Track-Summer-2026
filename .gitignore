# Dependencies
node_modules/

# Local workspace state (machine-specific timestamps)
openclaw-workspace-state.json

# OS / editor
.DS_Store
*.swp
*~

# Never commit secrets — use openclaw/config/openclaw.json.example as a template
openclaw/config/openclaw.json
.env
*.pem
*.key
